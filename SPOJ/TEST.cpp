#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    while(1){
        int x;
        cin>>x;
        if(x == 42) break;
        else cout<<x<<endl;
    }

    return 0;
}