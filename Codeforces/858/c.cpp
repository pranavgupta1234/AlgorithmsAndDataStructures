#include <bits/stdc++.h>

using namespace std;

string str;
int sz;

inline bool conso(char a){
	if(a == 'a' or a == 'e' or a == 'i' or a == 'o' or a == 'u'){
		return false;
	}
	return true;
}

inline bool check(int mid){
	if(conso(str[mid-1] and conso(str[mid]) and conso(str[mid+1])))
}

int main(){
	cin>>str;	
	sz = str.length();
	for(int i=1 ; i<n ; i++){

	}
	return 0;
}



