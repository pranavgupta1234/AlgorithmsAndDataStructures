#include <bits/stdc++.h>

#define ll long long

using namespace std;

bool isperf(ll x){
    return (int)sqrt(x)*(int)sqrt(x) == x;
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);    
    int t;
    cin>>t;
    while(t--){
        ll x;
        cin>>x;
        int n=1,m=1;
        for(int i=1 ; i<=floor(sqrt(x)+1) ; i++){
            
        }
        if(f) cout<<-1<<endl;
        else cout<<n<<" "<<m<<endl;
    }
    return 0;
}