#include <iostream>
#include <vector>

using namespace std;

struct node*{
	int data;
	node* next;
};

node* newNode(int data){

	node* newnode = new node();
	newnode -> data = data;
	newnode ->	next = NULL; 
	return newnode;
}

node* insert(node* newnode,int data){
	inser
}

int main(){
	
	node* cirll = 


	return 0;
}